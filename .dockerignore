node_modules
.next
.git
.gitignore
data
shot
*.log
# No documentation reaches the image. Excluding it also means a docs-only
# commit does not invalidate `COPY . .` and re-run the app build for nothing.
*.md
.env
.env.local
